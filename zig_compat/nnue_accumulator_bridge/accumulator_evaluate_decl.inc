void zfish_accumulator_evaluate(void*                  stack,
                                const void*            pos,
                                const void*            feature_transformer,
                                void*                  cache);
